function c = carro(x,y)
    % funcion que crea un carro en la posicion (x,y) donde (x,y) es
    % la mitad de la izquierda del rectangulo
    % largo = 4.1;
    % ancho = 1.7;
    %     h=fill([0,1,1,0],[0,0,2,2],'red'); constructor coordenada a
    %     coordenada

    % c = fill([x,x+largo,x+largo,x],[y-ancho/2,y-ancho/2,y+ancho/2,y+ancho/2],'black');
    plot(x,y,'.k','MarkerSize',30);
end